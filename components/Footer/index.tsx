import { Grid } from '@mui/material'
import styles from './styles.module.css'

function Footer() {
    return (
        <Grid container>
            <Grid item xs={12} className={styles.footerMain} >
                foooter
            </Grid>
        </Grid>
    )
}

export default Footer